# ASCII図解エディタ 作業記録・仕様・申し送り

最終更新: 2026-08-25

---

## 1. 要件(ユーザー要望の経緯)

### 当初要件
1. AIに図解で指示を出すための、**Obsidian上で動くエディタ**を作る
2. 罫線(`─` `└` `┐` `┤` など)、図形(`□` `◆` `●` など)、カギカッコ(`[　]` など)を**方眼紙のようなエディタに簡単に配置**できること
3. **コピペができる**こと。AIに指示を出す際にコピペしても、**エディタ上でイメージした位置関係・配置が崩れない**工夫をすること

### 追加要件(セッション中の追記)
4. 要素は**あらかじめ(パレットに)表示**されていること
5. エディタ上で要素を**選択・コピーしたら任意のマスにペースト**できること
6. **ドラッグ&ドロップ**対応
7. **カーソル(矢印)キーでも移動**できること

→ すべて実装済み。

---

## 2. 仕様

### 2.1 構成

| 項目 | 内容 |
|---|---|
| プラグインID | `ascii-diagram-editor`(表示名: ASCII図解エディタ) |
| ソース | `C:\Users\itamura\00_MyProjects\Obsidian-ascii-art-editor`(**こちらが正本**) |
| インストール先 | `C:\Users\itamura\Documents\Obsidian Vault\.obsidian\plugins\ascii-diagram-editor\` |
| ファイル | `manifest.json` / `main.js` / `styles.css`(+ `README.md` 利用者向け説明) |
| ビルド | 不要(プレーンJavaScript。TypeScript/esbuild未使用) |
| 起動 | リボンのグリッドアイコン、またはコマンド「ASCII図解エディタ: エディタを開く」 |

### 2.2 配置が崩れない仕組み(本プラグインの核)

1. **1マス = 全角1文字**に統一。空白マスは全角スペース(U+3000)で出力
2. 半角英数記号は入力・読み込み時に**自動で全角へ変換**(コードポイント+0xFEE0、半角スペース→U+3000)。設定でOFF可
3. どの行も「n文字目 = nマス目」となるため、フォントに関係なく**論理的な位置関係が必ず保持**される(AIは列位置で図を解釈する)
4. コピー時に**コードブロック(```)で囲む**(設定でOFF可)→ AIチャット/Markdownで等幅表示
5. 行末の全角スペースはコピー時に削除(配置に影響なし。設定でOFF可)

### 2.3 データモデル

- グリッド: `rows × cols`(初期値 40×20、5〜200)。セル値は `""`(空)または全角1文字の文字列
- 罫線の自動接続: 接続方向をビットマスクで表現(**上=1, 右=2, 下=4, 左=8**)。
  `STYLE_TABLES`(thin/bold/double)でマスク→文字、`CHAR_TO_MASK` で既存文字→マスクに変換し、OR合成で `├` `┼` `┬` 等を自動生成
- Undo/Redo: グリッド全体のJSONスナップショット方式(上限200件)
- 永続化: `settings.savedGrid` に自動保存(変更から800msデバウンス+ビュークローズ時)。再起動後も復元

### 2.4 ツール仕様

| ツール | 動作 |
|---|---|
| 選択 | ドラッグで矩形選択。選択範囲内をドラッグで移動(**Ctrl+ドラッグで複製**)、**矢印キーで1マス移動**(連打は800ms以内なら1つのUndo履歴に合体)。Ctrl+C/X/V、Delete、右クリックメニュー(コピー/切り取り/ここに貼り付け/削除/選択解除) |
| ペン | パレット選択文字をクリック・ドラッグで配置(高速ドラッグは線形補間で隙間なし)。右クリックで消去、**Alt+クリックでセルの文字を取得**(スポイト) |
| 消しゴム | クリック・ドラッグで消去 |
| 直線 | ドラッグで直線/L字線(横優先、**Shiftで縦優先**)。ゴーストプレビュー表示、既存罫線とマスク合成 |
| 四角 | ドラッグで枠描画。線種は「細/太/二重」セグメントで切替 |
| テキスト | セルクリックでキャレット表示、**日本語IME対応**(非表示inputをキャレット位置に配置、compositionendで確定)。Enter=改行(開始列に戻る)、Backspace/Delete/矢印/Esc対応 |
| スタンプ | パレットの複数マス要素(`［　］`「　」【　】箱小中大、矢印線)。クリックで連続配置、Escで解除 |

### 2.5 コピー&ペースト/ドラッグ&ドロップ仕様

- **範囲コピー(Ctrl+C)**: 内部クリップボード(`clipboardRegion`)+システムクリップボード(テキスト)の両方へ
- **ペースト(Ctrl+V)**: 内部リージョン優先、無ければシステムクリップボードのテキストを解釈。ホバー位置に**ゴーストプレビュー**を表示し、クリックで確定。Escでキャンセル
- **透過仕様**: ペースト/スタンプ/移動時、リージョン内の空白マスは下の内容を上書きしない(枠だけ被せる用途に対応)
- **DnD**: パレット文字・スタンプは `draggable`。グリッドの `dragover` でゴースト表示、`drop` で配置。外部からのテキストドロップも同様に解釈
- **読み込み**: クリップボードのテキスト(コードフェンス付き可)をグリッド全体へ展開。必要ならグリッドを自動拡張(上限200)
- **全体コピー/ノートへ挿入**: 図全体を出力。挿入先は最後にアクティブだったMarkdownビュー(`active-leaf-change`で追跡)

### 2.6 キーボード処理(重要な設計判断)

- リスナーは **document レベル**に登録し、`isKeyboardTarget()` でガード:
  `activeLeaf === this.leaf` かつ「フォーカスが body またはビューコンテナ内」のときだけ処理
- 理由: ビュー内要素にリスナーを付けると、Obsidianがフォーカスをビューコンテナへ移した際にイベントが届かない(§4 申し送り参照)
- グリッドでのドラッグ操作後は `gridWrapEl` へ再フォーカス(setTimeout 0)

### 2.7 表示(CSS)

- セルは固定サイズのCSS Grid。**セルの font-size = セル幅(`--aae-cell`)** に設定
  → 全角グリフの字送り(1em)がセル幅と一致し、罫線が途切れず繋がる
- フォントスタック: MS Gothic 系優先(罫線・図形が全角幅で描画される)
- Obsidianテーマ変数(`--interactive-accent` 等)使用でダーク/ライト両対応
- 5マスごとに方眼の補助線を濃く表示。ズーム(セル14〜48px)対応

---

## 3. 作業記録(2026-08-25)

1. **環境確認**: Vault「Obsidian Vault」(`C:\Users\itamura\Documents\Obsidian Vault`)を特定。Obsidian CLI・Node.js利用可能を確認
2. **初版実装**: manifest.json / main.js / styles.css を作成し、Vaultへコピー、有効化
   - 注意点: コピー直後は `app.plugins.loadManifests()` を呼ばないと有効化できなかった
3. **機能テスト(evalによる)**: スタンプ配置、罫線自動接続(`├` `┼` が正しく生成)、Undo/Redo、範囲選択→コピー→ペースト、矢印キー移動 — すべて成功
4. **不具合修正**:
   - CSSの `nth-child` ハックと不正な `color-mix` 値を修正
   - パレットの最大高さを拡大し全カテゴリを常時表示(追加要件4に対応)
   - エディタ上で罫線が破線状に見える → セルの font-size をセル幅と同一にして解消
   - **矢印キーが効かない**(ユーザー報告)→ キーリスナーを rootEl から document レベル+アクティブビューガードへ変更して解消
5. **検証**: `dev:errors` クリーン、スクリーンショットで表示確認、別ペインアクティブ時に誤作動しないことを確認
6. **ドキュメント**: README.md(利用者向け)、本書(HANDOVER.md)作成

---

## 4. 申し送り事項

### 開発サイクル
```powershell
# ソースを編集したら:
Copy-Item "C:\Users\itamura\00_MyProjects\Obsidian-ascii-art-editor\*.js","...\*.css","...\*.json" "C:\Users\itamura\Documents\Obsidian Vault\.obsidian\plugins\ascii-diagram-editor\" -Force
obsidian plugin:reload id=ascii-diagram-editor
obsidian dev:errors
```
- **ソース側(00_MyProjects)が正本**。Vault側はコピーなので、編集は必ずソース側で行いVaultへ反映すること
- **gitは未初期化**。履歴管理を始めるなら `git init` を推奨

### ハマりどころ(再発防止)
- **`obsidian dev:screenshot` は古いフレームを返すことがある**。変更確認時は再描画を強制し(スクロール操作等)、少し待ってから撮り直す。DOM/computed styleでの確認を優先
- **evalによるテストは実際のフォーカス遷移を再現しない**。矢印キー問題は「evalでは動くが実操作で動かない」パターンだった。キーボード系の変更は必ず実操作で確認してもらうこと
- Obsidianはクリック後にフォーカスをビューコンテナへ移すことがある → キーイベントはdocumentレベルで受けてガードする(§2.6)。`activeLeaf` APIは非推奨傾向のため、将来のObsidianアップデートで動作を確認すること

### 既知の制限・仕様上の割り切り
- 矢印・図形・罫線はUnicodeの曖昧幅(Ambiguous Width)文字のため、**CJKフォントでない環境では見た目の幅がずれる**ことがある。ただし1マス=1文字の原則により論理位置は常に保たれ、AIへの伝達には影響しない
- ペースト/移動/スタンプの**空白マスは透過**(意図的な仕様。下の内容を残す)
- グリッド内容は `settings.savedGrid` に1枠のみ保存。エディタを複数タブで開くと最後に保存した内容が勝つ
- モバイル(`isDesktopOnly: false`)は未検証
- 実マウスでのドラッグ描画・パレットDnD・IME実入力は、コード上は実装済みだがCLIでは自動テスト不可。ユーザー実操作での不具合報告ベースで対応中(現時点で報告済み不具合はすべて解消)

### 今後の拡張候補(未着手)
- 直線ツールの端点に矢印(`→` 等)を自動付与するオプション
- 選択範囲の左右反転・回転(罫線文字の対応変換が必要)
- 複数図面の保存スロット/ノートのコードブロックを直接開いて編集する機能
- 半角モード(1マス=半角1文字)の切替
